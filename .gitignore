# Rust / Cargo build outputs
target/
**/*.rs.bk
Cargo.lock.bak

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# Tauri
.tauri/
src-tauri/WixTools/
src-tauri/target/

# Icons — tracked in git so CI can build without extra setup steps
# assets/icons/*.png  (intentionally NOT excluded)
# assets/icons/*.ico  (intentionally NOT excluded)
# assets/icons/*.icns (intentionally NOT excluded)

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# Editor / IDE
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
.env
.env.local
